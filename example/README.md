# flutter_opengl example

Demonstrates flutter_opengl rendering a ShaderToy shader on all supported platforms.

## Running

```bash
flutter run -d linux    # or chrome, windows, macos, android, ios
```
